import React from "react";

export const Greet = (props) => {
  const { name, children } = props;
  return (
    <div>
      <h1>Na paka Gwapo mo {name}</h1>
      <button>nigger</button>
      {children}
    </div>
  );
};
